name: RDP
on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      - name: Set RDP password
        run: net user runneradmin "P@ssw0rd123!" /Y
        shell: cmd

      - name: Enable RDP and disable NLA
        shell: powershell
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

      - name: Download ngrok
        shell: powershell
        run: |
          Invoke-WebRequest -Uri "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip" -OutFile "ngrok.zip"
          Expand-Archive -Path "ngrok.zip" -DestinationPath "."
          .\ngrok.exe config add-authtoken ${{ secrets.NGROK_TOKEN }}

      - name: Start ngrok
        shell: powershell
        run: |
          Start-Process .\ngrok.exe -ArgumentList "tcp 3389" -WindowStyle Hidden
          Start-Sleep -Seconds 15
          $tunnel = (Invoke-RestMethod http://127.0.0.1:4040/api/tunnels).tunnels[0].public_url
          Write-Host "================================================"
          Write-Host "RDP ADDRESS: $tunnel"
          Write-Host "USERNAME: runneradmin"
          Write-Host "PASSWORD: P@ssw0rd123!"
          Write-Host "================================================"

      - name: Keep alive
        shell: powershell
        run: |
          for ($i=0; $i -lt 350; $i++) {
            Start-Sleep -Seconds 60
            $alive = $false
            try {
              $r = Invoke-RestMethod http://127.0.0.1:4040/api/tunnels
              if ($r.tunnels.Count -gt 0) { $alive = $true }
            } catch { }
            if (-not $alive) {
              Start-Process .\ngrok.exe -ArgumentList "tcp 3389" -WindowStyle Hidden
              Start-Sleep -Seconds 15
              $t = (Invoke-RestMethod http://127.0.0.1:4040/api/tunnels).tunnels[0].public_url
              Write-Host "RESTARTED ngrok at minute $i. NEW ADDRESS: $t"
            }
          }
